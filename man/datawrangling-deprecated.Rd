% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code.categorical.R,
%   R/datawrangling-deprecated.r, R/duplicates.remove.R, R/files.bind.R,
%   R/files.join.R, R/files.merge.R, R/remove.latent.R, R/remove.save.R,
%   R/reshape.gather.R, R/reshape.spread.R
\name{datawrangling-deprecated}
\alias{datawrangling-deprecated}
\alias{code.categorical}
\alias{duplicates.remove}
\alias{files.bind}
\alias{files.join}
\alias{files.merge}
\alias{remove.latent}
\alias{remove.save}
\alias{reshape.gather}
\alias{reshape.spread}
\title{Deprecated functions in package \pkg{datawrangling}.}
\usage{
code.categorical(x, variable = "", order = c())

duplicates.remove(x, timing.variables = "SessionStartDateTimeUtc",
  subj.variable = "Subject", taskname = "", output.folder = "")

files.bind(path = "", pattern = "", delim = "\\t", na = "",
  output.file = "", bind = "rows")

files.join(path = "", pattern = "", delim = "\\t", na = "",
  output.file = "", id = NULL)

files.merge(path = "", pattern = "", delim = "\\t", na = "",
  output.file = "", id = NULL)

remove.latent(x, factor.list, missing.allowed, id, output.dir = NULL,
  output.file = NULL)

remove.save(x, remove, output.dir = NULL, output.file = NULL,
  id = "Subject")

reshape.gather(x, variable.names, values, id = NULL,
  separate.pattern = NULL)

reshape.spread(x, variables = "", variables_combine.sep = "_",
  values = c(), id = "", fill = NA)
}
\description{
The functions listed below are deprecated and will be defunct in
  the near future. When possible, alternative functions with similar
  functionality are also mentioned. Help pages for deprecated functions are
  available at \code{help("<function>-deprecated")}.
}
\section{\code{code.categorical}}{

For \code{code.categorical}, use \code{\link{code_categorical}}.
}

\section{\code{duplicates.remove}}{

For \code{duplicates.remove}, use \code{\link{duplicates_remove}}.
}

\section{\code{files.bind}}{

For \code{files.bind}, use \code{\link{files_bind}}.
}

\section{\code{files.join}}{

For \code{files.join}, use \code{\link{files_join}}.
}

\section{\code{files.merge}}{

For \code{files.merge}, use \code{\link{files_join}}.
}

\section{\code{remove.latent}}{

For \code{remove.latent}, use \code{\link{remove_latent}}.
}

\section{\code{remove.save}}{

For \code{remove.save}, use \code{\link{remove_save}}.
}

\section{\code{reshape.gather}}{

For \code{reshape.gather}, use \code{\link{reshape_gather}}.
}

\section{\code{reshape.spread}}{

For \code{reshape.spread}, use \code{\link{reshape_spread}}.
}

\examples{
code.categorical(x, variable = "columnName", order = c("string1", "string2", "string3"))
duplicates.remove(x, subset = c(colnames(x)), context = "Subject")
files.bind(path = "./Data", pattern = ".txt")
files.join(path = "./Data", pattern = ".txt")
files.merge(path = "./Data", pattern = ".txt")
# Example
remove.save(data, remove = data_remove, save = "data/remove", taskname = "Flanker")
reshape.gather(x, variables = "variable", values = c("value1", "value2"), by = "Subject")
reshape.spread(x, variables = "variable", values = c("value1", "value2"), by = "Subject")
}
\keyword{code}
\keyword{internal}
\keyword{remove}
\keyword{reshape}
